# 관리자 페이지 프롬프트 (타로 · 럭키드로우) — 내용 중심

> 디자인(색·폰트·여백·그림자 등)은 **지정하지 않는다.** 아래는 각 화면에 **반드시 들어가야
> 하는 내용·데이터·동작**만 정리한 명세다. 시각 디자인은 만드는 쪽이 알아서 정한다.
> 모든 문구는 한국어, UI 에 금액·판매 표현은 넣지 않는다.

---

## 지시문 (이 아래를 그대로 전달)

**생일카페 이벤트 페이지를 슬롯 단위로 파는 플랫폼**의 **주최자(고객) 관리자 페이지**를
만들어라. 아래 화면들이 담아야 할 **내용과 동작**을 충실히 구현하면 된다. 시각 스타일은 자유.

### 0. 맥락

- **주최자**는 자기 이벤트의 **운영 데이터와 자기 계정만** 관리한다. 테마·색·이미지는 플랫폼
  소유자가 정하므로 관리자 화면에서 건드리지 않는다.
- 서비스는 두 가지다: ① **타로**(질문·답변 관리) ② **럭키드로우**(경품 추첨 운영). 두
  서비스의 관리자 화면은 **같은 셸**(좌측 메뉴 + 본문, 로그인/로그아웃)을 쓰고 내용만 다르다.
- 메뉴는 **최소로**. 럭키드로우는 운영에 필요한 걸 **대시보드 한 화면**에 모으고 `배송 정보`·
  `내 계정`만 별도 메뉴로 뺀다. 타로는 `질문 타로`·`내 계정`.

### 1. 공통 셸

- 좌측: 이벤트 이름 + `/{슬러그} · 관리`, 그 아래 메뉴, 맨 아래 로그인 이메일 + `로그아웃`.
- 메뉴 항목마다 `내 페이지 보기`(방문자 화면 새 탭) 링크 포함.
- 로그인해야 들어갈 수 있고, 로그아웃하면 즉시 로그인 화면으로.

### 2. 화면: 럭키드로우 — 대시보드 (한 화면)

메뉴: **대시보드**(현재) · 배송 정보 · 내 계정 · 내 페이지 보기.

**(1) 상단 지표 3가지**
- 남은 수량 합계 (+ 상품 종수)
- 오늘 나간 수량 (+ 누적 나간 수량)
- 운영 상태: `리허설` / `실제 운영` / `마감` 중 하나 + 짧은 설명

**(2) 상품과 수량 (표)** — 손이 제일 자주 가는 곳이라 먼저 배치
- 열: **등수**(행 순서 = 등수), **상품명**(입력), **남은 수량**(숫자 입력), **오늘 나감**
  (읽기 전용·자동 집계), **누적**(읽기 전용·자동 집계), **배송 필요 여부**(체크), 삭제
- 묶음 제한이 켜져 있으면 열 하나 추가: **5개당 최대**(숫자 입력, 비우면 "제한 없음")
- `상품 추가` 버튼, 하단에 합계(남은 수량·오늘 나감)
- **명시적 저장:** 여러 칸을 고친 뒤 저장 버튼으로 한 번에 저장. 저장 안 하고 나가려 하면 경고.
  버튼 상태: `변경사항 저장` / `저장 중…` / `저장됨`. 저장 버튼은 표가 길어도 늘 닿게.
- 안내 배너: 설정이 잠겨 있으면 "잠금을 풀어야 고칠 수 있어요", 리허설이면 "재고가 줄지 않아요".

**(3) 운영 설정** — 여기 값은 **누르는 즉시 저장**된다 (저장 버튼 없음)
- 지금 상태(각각 켜고 끔):
  - **실제 운영**: 기본은 꺼짐(리허설). 리허설에선 뽑아도 재고가 안 줄고, 켜면 실제로 줄어든다.
    켤 때는 **확인창**("실제 운영으로 바꿀까요? …").
  - **행사 마감**: 켜면 방문자가 추첨 못 함. 켤 때 **확인창**.
  - **설정 잠금**: 상품·수량을 못 고치게 잠금(추첨은 그대로).
- **당첨 결과 표시**: 손님에게 보일 방식 — `등수와 상품명` / `등수만` / `상품명만` 중 택1.
- **묶음 뽑기 제한**: 켜면 위 상품 표에 '5개당 최대' 열이 생겨 상품마다 상한을 정할 수 있다.
  켜면 "흔한 상품이 덜 나오는 만큼 비싼 상품이 더 빨리 소진된다"는 경고를 함께 보여준다.

### 3. 화면: 럭키드로우 — 배송 정보

메뉴: 대시보드 · **배송 정보**(현재) · 내 계정.
- 상단: 제출 건수 + "배송이 끝나면 반드시 지워주세요"(개인정보).
- 액션: `CSV 내려받기`, `전체 삭제`(둘 다, 데이터 없으면 비활성).
- 안내: 마감 +14일까지 열람 가능, +15일이 지나면 자동 삭제.
- 표: 이름 · 연락처 · 주소 · 당첨 상품 · 제출 시각. 비어 있으면 "아직 제출된 배송 정보가 없어요."

### 4. 화면: 타로 — 질문 타로 (목록)

메뉴: **질문 타로**(현재) · 내 계정 · 내 페이지 보기.
- 상단: 제목 + 설명("방문자가 고를 질문과, 카드가 나왔을 때 보여줄 답변을 관리해요"), `질문 추가`.
- 질문마다 한 줄: **공개 여부**(체크·즉시 반영), 질문 제목, 보조 정보("메이저 22장 · N개 답변
  입력됨"), 삭제, 클릭하면 **질문 편집** 화면으로.
- 질문이 없으면 "아직 질문이 없어요. 위에서 추가해 보세요."

### 5. 화면: 타로 — 질문 편집

- 상단에 `‹ 질문 목록` 뒤로가기.
- **질문**: 방문자에게 보일 질문 문구 입력, 공개 여부 체크.
- **뽑기 설정**: 덱(메이저 22장 / 전체 78장 등), 역방향 허용 여부.
- **카드별 답변**: 해당 덱의 카드(22 또는 78장)마다 답변 입력칸. 안 채운 카드는 기본 카드
  의미로 대체된다는 안내. (답변 편집은 **즉시 저장** — 저장을 잊어 날리는 게 더 나쁘다.)

### 6. 화면: 로그인 (공통)

- 이벤트 이름 + `/{슬러그} 관리자`, 이메일·비밀번호 입력, `로그인`.
- 로그인 실패 사유를 사람 말로 구분해 보여줌(비밀번호 틀림 / 이 슬롯 관리자 아님 등).

### 7. 공통 동작 규칙 (요약)

- **상품 표 = 명시적 저장**(경고 있음) / **운영 설정·질문 공개·카드 답변 = 즉시 저장**.
- **오늘 나감·누적**은 읽기 전용(자동 집계) — 손으로 못 고침.
- **5개당 최대**는 개수 입력, 비우면 제한 없음.
- 되돌리기 어려운 동작(실제 운영 전환, 마감, 전체 삭제)은 **확인창**으로 한 번 더 묻는다.
