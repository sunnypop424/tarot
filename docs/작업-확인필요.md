# 확인 결과와 남은 일

작성: 2026-07-27 밤 → **2026-07-28 확인 반영**

지난 문서는 "자고 일어나서 확인할 것" 이었고, 이제 그 확인이 끝났습니다.
이 문서는 이제 **①확인에서 나온 고칠 것** 과 **②아직 안 본 것** 과 **③남은 작업**입니다.

---

## 1. 확인에서 나온 것 — **넷 다 고쳤습니다**

체크하시면서 남기신 코멘트에서 나온 것들입니다. 전부 고쳐서 배포했습니다.

### 1-1. "저장" 이 공유로 뜬다 — **세 서비스 공통** ✔ 고침

> :: 들어가긴 하는데 저장이 아니라 공유가 되는거 같아. 다른 서비스들도 한번 확인해봐.
> :: (포토존) 얘도 공유하기로 뜨는듯.

말씀대로입니다. 코드를 보니 원인이 하나고 **포토존·모의고사·포토카드가 전부 같은 함수를
씁니다** (`src/lib/compose.ts` 의 `saveResult`).

지금 순서가 **공유 → 다운로드 → 새 탭** 입니다. iOS Safari 가 `<a download>` 를 무시하는
경우가 있어서 공유를 먼저 뒀는데, **안드로이드·데스크톱은 공유가 되니까 거기서 멈춰버립니다.**
그래서 어디서 눌러도 공유 시트가 뜹니다.

게다가 모의고사는 **'저장' 과 '공유' 버튼이 같은 함수를 부릅니다** — 두 버튼이 글자만 다르고
하는 일이 똑같아요. 이건 그냥 제 실수입니다.

**고친 방식:** 순서를 뒤집어 **저장은 다운로드 먼저, 공유는 공유 먼저** 로 갈랐습니다
(`saveResult` / `shareResult`). iOS 처럼 다운로드가 실제로 안 되는 데서만 저장이 공유로
물러섭니다. 포토존도 두 버튼이 같은 함수였어서 같이 고쳤고, 포토카드에는 공유 버튼이
아예 없어서 새로 넣었습니다(공유가 되는 기기에서만 뜹니다).

iOS 판별을 따로 넣었습니다 — Safari 는 `download` 속성을 **지원한다고 말하면서 무시합니다.**
아이패드는 데스크톱으로 위장하므로 터치 지원 여부까지 봅니다.

*확인:* 저장 → `download:...png`, 공유 → `share` 로 서로 다른 경로를 탑니다.

### 1-2. 칭호 카드 글자가 카드 밖으로 튀어나온다 ✔ 고침

> :: 글자가 카드보다 튀어나와.

화면에 보이는 카드 쪽입니다. 긴 칭호에 글자 크기를 줄이는 장치는 **저장되는 이미지(캔버스)**
에만 넣었고 화면 카드엔 안 넣었습니다 — 두 곳이 같은 값을 그리는데 한쪽만 방어한 셈입니다.

**고친 방식:** 캔버스가 하던 것과 같은 일(넘치면 폰트를 줄인다)을 화면에서도 합니다.
CSS 만으로는 안 됩니다 — `clamp()` 는 화면 폭만 보고 **글자 길이는 모릅니다.**
"찐팬 인증" 이 들어가는 크기가 "우주 최강 초통령 덕후" 는 안 들어가요.

*확인:* 12글자짜리 칭호가 카드를 0px 넘칩니다 (폰트가 40 → 39px 로 줄고 줄바꿈).

### 1-3. 등불 실루엣 PNG 를 올리면 글자가 등불 밖으로 나간 것처럼 보인다 ✔ 고침

> :: 등불모양 실루엣 png 를 올리면 모양대로 잘리니까 안에 내용도 같이 잘려서
> 텍스트가 제대로 안보일때가 많아.

처음엔 마스크가 글자까지 자르는 줄 알았는데, 짚어주신 대로 **글자는 안 잘리고 글자 밑의
배경이 투명해서** 등불 밖으로 나간 것처럼 보이는 거였습니다. 원인이 둘이라 둘 다 고쳤습니다.

**고친 방식 ①** 마스크를 등불 자체가 아니라 **배경 층(`::before`)에만** 겁니다 — 그래야
글자가 안 잘립니다.
**고친 방식 ②** 말씀하신 대로 **네 면 여백을 편집기에서 직접 잡게** 했습니다.
처음엔 "가운데가 넓을 테니 가운데로 모으자" 로 짐작했는데, 실루엣 모양은 PNG 마다 다릅니다
(호리병·원형·물방울·사각형) — **코드가 짐작할 수 있는 게 아니라 눈으로 맞출 일**이에요.

편집기 소원나무 카드에 **실루엣을 올렸을 때만** '실루엣 안 글자 자리' 가 나오고,
위·오른쪽·아래·왼쪽을 등불 크기의 %로 잡습니다. 미리보기를 보면서 맞추시면 됩니다.

정렬은 **위 맞춤**입니다 — 가운데 정렬로 뒀더니 글이 짧을 때 위아래 값을 다르게 줘도
가운데로 밀려나 **조절이 안 먹는 것처럼 보였습니다.** 위 맞춤이면 '위' 값이 곧 글자가
시작하는 자리라 눈으로 맞추기 쉽습니다.

### 1-4. 등불이 길게 떨어지면서 페이지 번호를 가린다 ✔ 고침

> :: 등불이 길게 떨어지면서 pagination을 가릴때가 있음.

줄 길이를 계산할 때 **등불 아래로 삐져나오는 것**을 안 뺐습니다. 장식(charm)이 등불 바닥에서
16px 내려가 22px 더 걸리는데, 좌우 잘림은 고쳤으면서(흔들림 폭) 아래쪽은 안 봤어요.

**고친 방식 ①** 장식이 있는 소원은 아래로 42px, 없으면 12px 을 미리 빼고 자리를 잡습니다
(겹침 판정에도 같이 반영).

**고친 방식 ②** 겹쳐 보이지 않아도 **클릭을 못 하는** 문제가 남아 있었습니다 (짚어주신 대로).
등불의 `z-index` 가 줄 길이만큼 올라가서, **투명한 여백이 페이지 번호 위를 덮고 클릭을
가로챘습니다** — 눈으로는 안 잡히는 종류입니다. 나무의 등불은 전부 장식이라 누를 게 없으니
아예 클릭을 안 먹게 하고, 페이지 번호는 무엇이 깔리든 위로 오게 했습니다.

*확인:* 등불 9개가 페이지 번호를 0px 침범하고, 페이지 번호 버튼이 **실제로 클릭 대상**입니다
(`elementFromPoint` 로 확인).

---

## 2. 확인이 끝난 것

지난 문서 §3 의 체크리스트입니다. 아래 둘만 빼고 전부 확인해 주셨습니다.

**아직 안 본 것 (실기기라야 압니다):**

- [ ] **아이폰에서 포토존 저장** — `<a download>` 가 무시되면 "길게 눌러 저장" 안내가 떠야 합니다
- [ ] **아이폰 카메라 전체화면 튐** — `playsInline` 을 넣었지만 iOS 버전마다 다릅니다

※ 1-1 을 고쳤으니 이제 이 둘을 보실 수 있습니다. 아이폰에서 '저장' 을 누르면 다운로드가
안 먹어서 **공유 시트로 물러서는 게 정상**이고, 그것도 안 되면 새 탭이 열리면서
"길게 눌러 저장" 안내가 떠야 합니다.

**확인해 주신 것** — 전·후면 전환, 소원나무 흔들림, 편집기 서비스 카드 다섯 종, 저장 전/후
미리보기, 스탬프 현장 운영 4가지, 포토카드 모드 3종 전부, 모의고사 재채점.

---

## 3. 확인 코멘트를 받아 **이미 반영한 것**

### 3-1. 스탬프 도장 이미지 — 넣었습니다

> :: 각 스탬프 이벤트 별로 도장 이미지 업로드 필요.

편집기의 칸마다 투명 PNG 를 올릴 수 있습니다. 안 올리면 지금처럼 내장 아이콘이 돌아가며
들어가고 **도장색**을 따르고, 올리면 그림의 색이 그대로 나갑니다 (그래서 도장색이 안 먹어요 —
화면에도 그렇게 적어뒀습니다). 그림을 바꾸거나 지우면 Storage 원본도 같이 지웁니다.

### 3-2. 그 밖에 이번에 같이 한 것

- **모의고사 칭호에 그림(PNG)** — 칭호 위에 뜨고 저장되는 카드에도 같이 들어갑니다
- **포토카드 '럭키' 표시** — 주최자가 체크하면 스태프 화면 카드 목록에 별이 붙습니다.
  레어도(확률)와 **따로** 뒀어요: 레어도 5인데 굳이 강조 안 하고 싶은 카드도, 레어도 2인데
  팬들이 아는 컷이라 강조하고 싶은 카드도 있어서요
- **스태프 화면을 `/{slug}/staff` 로 분리** — 판매·1장 증정 둘 다. 부스 기기에 관리 사이드바가
  뜨면 손님이 누르고, 손님이 같이 보는 화면이라 슬롯 색을 씁니다
- **뽑기권 목록** (발급 수·번호별 뽑힌 카드·삭제·CSV)
- **수령 목록** — 스캐너뿐이라 "몇 장 나갔고 몇 장 남았나" 를 볼 길이 없었습니다
- **응모자 화면에 "자격은 얻었는데 아직 안 낸 분"** — 그분들은 연락처가 없어 추첨 후보에도
  안 들어가는데 그 사실이 화면 어디에도 없었습니다
- **포토카드 레어도·재고를 편집기에서 빼고 주최자 화면으로** — 확률은 행사 반응을 보고
  조정하는 값인데 그때마다 저를 부르시면 현장에서 못 고칩니다
- **카드 섞기** — 제자리에서 떨기만 하던 걸 가운데로 모였다 펼쳐지게 고쳤습니다
  (실측 이동거리 34px → 219px)

---

## 4. 남은 작업

### 4-1. 포토카드 설정을 럭키드로우 수준으로

> 수량 picker 등의 디자인은 럭드와 동일하게 설정 가능하게, 배경도 로고도.

럭드는 배경 사진 위에 박스를 얹고 그 박스의 여백·그림자·테두리·모달 색·수량 입력칸 색까지
전부 편집기에서 고를 수 있습니다. 포토카드는 지금 색 여섯 개뿐이라 그 수준으로 올립니다.

### 4-2. 편집기 미리보기에서 **모든 화면** 보기

> 다른 서비스들도 미리보기에서 모든 화면을 볼 수 있어야해
> (주최자 페이지 제외, 사용자 화면 스태프 화면은 미리보기 제공을 해야함)

지금은 럭드(뽑기/당첨/전체결과)와 롤페·소원나무(벽/작성)만 화면을 고를 수 있고 나머지는 첫
화면만 보입니다 — **색을 고르는 사람이 결과·보상 화면을 못 보고 배포한다는 뜻**이라 고칩니다.

화면 목록은 한곳에 모아뒀습니다 (`src/owner/previewScreens.ts`). 각 앱이 그 상태를 읽어
화면을 고정하는 연결이 남았어요. 스태프 화면도 목록에 넣었습니다.

### 4-3. 배포 순서 주의 (안 바뀜)

`SLOT_FIELDS` 에 컬럼을 추가한 프론트가 DB보다 **먼저** 나가면 그 사이 모든 슬롯이 400 으로
안 열립니다. **마이그레이션이 항상 먼저입니다.**

---

## 5. 여쭤볼 것

### Q1. 포토카드 — **(가) 로 만들었습니다**

앞서 여쭤본 두 갈래 중 **(가) 운영 흐름만 럭드에 맞추고 화면은 시안** 으로 갔습니다.
스태프 기기·연습 모드·마감·묶음 상한은 럭드와 똑같은 개념이고, 덱·결과 연출은 시안 그대로예요.
따로 답 주신다고 하셨으니 그때 맞추겠습니다.

### Q1-b. 포토카드 이미지의 권리 — **결정이 필요합니다**

이건 제가 정할 수 없는 항목이라 남깁니다. 아이돌 사진을 뽑아서 **저장까지** 하게 되는데,
초상권(연예인)·저작권(소속사 공식 사진, 홈마 사진)이 전부 남의 것입니다.
럭드 경품은 물건이라 이 문제가 없었고, 타로 앞면은 주최자가 만든 것이었어요 —
**플랫폼이 처음 마주하는 종류의 위험입니다.**

지금 넣어둔 것: 편집기에 "사용 권리를 확인한 이미지만" 경고, 주최자 안내문에도 같은 문장,
슬롯을 지우면 이미지까지 완전 삭제. **법적 판단은 제 범위 밖이라 출시 전에 정하셔야 합니다.**

---

## 6. 정해진 것 (더 안 여쭙습니다)

- **소원나무 배경 이미지를 올리면 별·가지는 숨깁니다** — 그대로 둡니다
- **롤링페이퍼 페이지 나누기** — 지금 도는 이벤트가 없으니 그대로 갑니다
- **모의고사 서술형** — 뺍니다. 나중에 붙이려면 문항 유형만 늘리면 되게 열어뒀습니다
- **`SUPABASE_ACCESS_TOKEN`** — 직접 폐기하십니다. 제가 쓴 건 `supabase/migrations/` 의
  SQL 실행뿐이고 적용 이력은 `public._applied_migrations` 에 남습니다

---

## 7. 지난 문서에서 정한 것들 (그대로 유효)

역할 경계는 이렇게 갈라 뒀습니다. 뒤집고 싶으시면 말씀해 주세요.

| 서비스 | 편집기 (담당자) | 주최자 화면 |
|---|---|---|
| 스탬프 | 칸 개수·이름·도장 그림, 색·문구 | 현장 암호, 보상 방식, 자정 리셋, 마감 |
| 모의고사 | 칭호(이름·그림), 색·문구 | 문항·정답·커트라인·제한시간·정답 공개 |
| 포토카드 | 카드 이미지·이름, 색·문구 | **레어도·재고·럭키**, 운영 방식, 연습, 마감 |
| 포토존 | 프레임 PNG, 색·문구 | (없음 — 안내 한 장) |
| 소원나무·롤페 | 등불/쪽지 색·모양, 배경 | 후검수 (숨기기·삭제) |
| 투표 | 색·문구·결과 표시 방식 | 설문·선택지·공개·마감 |

가른 기준은 하나입니다: **행사 중에 바뀌는 값이면 주최자, 배포 전에 정하는 값이면 편집기.**
예외가 하나 있는데 **이미지 업로드는 무조건 편집기**입니다 — Storage 쓰기가 담당자 계정으로만
잠겨 있고(방문자 사진 호스팅으로 번지지 않게) 그 잠금을 안 푸는 게 이 플랫폼의 설계입니다.

그 밖에 지난 문서에서 설명한 판단들(소원나무 등불 배치 규칙·빛번짐 세기·한 페이지 = 한 화면·
칭호 백분율 기준·보상 켜면 재응시 강제 차단·보기 섞기 제외·포토존 촬영 화면 고정 다크)은
그대로입니다.

---

## 8. 알아두실 것

- **`scripts/db.mjs`** 로 마이그레이션을 적용합니다. `--pending` 으로 안 된 것 확인, `--migrate` 로 적용
- **`docs/DESIGN_PROMPT_SERVICES.md`** — 6개 서비스 디자인 프롬프트 (화면을 더 뽑으실 때)
- **테스트 슬롯**: `treetest`(소원나무)·`phototest`(포토존)·`rollingtest`(롤페)·`stamptest`·
  `quiztest`·`pctest`. 편집기에서 지우시면 딸린 데이터까지 같이 지워집니다
- **개발 서버(Vite)가 가끔 옛 화면을 보여줍니다** — 새 export 나 CSS 를 추가할 때 나는
  현상이고 배포본에는 없습니다. 화면이 이상하면 개발 서버를 한 번 재시작해 보세요
  (이번 작업 중 세 번 있었고 전부 그거였습니다)
