/**
 * 문의 양식 — **손님이 오픈채팅에 붙여넣을 글**을 여기서 만든다.
 *
 * 문의는 오픈채팅으로만 받는다. 폼을 서버로 보내지 않는 게 요점이라 이 파일은 **문자열만**
 * 만들고, 보내는 일은 손님의 카톡이 한다 (우리 쪽에 저장되는 개인정보가 0이 된다).
 *
 * 서비스별 칸은 **실제로 우리가 받아야 하는 것만** 적는다. 관리자 페이지에서 손님이 직접
 * 넣는 것(경품·문항·카드)은 칸을 만들지 않고 `※` 로 그렇게 안내한다 — 양식이 길어지면
 * 아무도 안 채운다.
 */
import type { ServiceId } from '@/data/services'

export const KAKAO_URL = 'https://open.kakao.com/o/skxgV2Th'

/** 오픈채팅 별명 규칙 — 방에 들어오자마자 하는 일이라 화면 첫 줄에 세운다 */
export const NICKNAME_RULE = '행사기간 / 멤버이름'
export const NICKNAME_EXAMPLE = '8/12~8/14 / 디노'

const LINE = '------------------------'

/**
 * 서비스별 칸. **`Record<ServiceId, …>` 라 서비스가 늘면 여기가 컴파일 에러로 터진다** —
 * 양식만 조용히 빠지는 일이 없다 (이 레포의 다른 소비처들과 같은 장치).
 */
export const SERVICE_FORM: Record<ServiceId, { name: string; lines: string[] }> = {
  tarot: {
    name: '타로카드',
    lines: [
      '뽑는 장수 (1장 / 3장):',
      '카드 앞면 (기본 제공 78장 / 직접 제작):',
      '카드 뒷면 이미지:',
      '질문 타로 사용 여부:',
      'AI 종합 리딩 사용 여부 (3장 뽑기에만 붙습니다):',
      '※ 질문과 답변은 운영 전 관리자 페이지에서 직접 입력해 주시면 됩니다.',
    ],
  },
  luckydraw: {
    name: '럭키드로우',
    lines: [
      '사용할 기기 (예: 아이패드 에어, 아이패드 미니 등):',
      '뽑기 버튼 문구 (기본 DRAW):',
      '배송이 필요한 경품 유무 (있으면 당첨자 배송 정보 입력 화면이 붙습니다):',
      '※ 물품 및 수량은 운영 전 관리자 페이지에서 직접 입력해 주시면 됩니다.',
      '　(물품 최대 100개, 수량 제한 없음, 임의 확률 조정 불가)',
    ],
  },
  photocard: {
    name: '포토카드 뽑기',
    lines: [
      '운영 방식 (저장용 / 1장 증정 / 판매 N연차):',
      '사용할 기기 (증정·판매는 스태프 기기에서 뽑습니다):',
      '카드 뒷면 이미지:',
      '실물 카드 교환 여부:',
      '※ 카드 이미지·이름·레어도·재고는 관리자 페이지에서 직접 등록해 주시면 됩니다.',
      '※ 저장용은 손님 폰에서 뽑고 이미지 저장까지 됩니다.',
    ],
  },
  rolling: {
    name: '롤링페이퍼',
    lines: [
      '벽(스크린) 사용 여부와 기기:',
      '쪽지 색·글씨체 희망:',
      '스티커 이미지 (있다면 오픈채팅으로 전달):',
      '공개 전 검수 여부 (켜면 승인한 쪽지만 벽에 붙습니다):',
    ],
  },
  wish: {
    name: '소원 나무',
    lines: [
      '나무·밤하늘 배경 이미지:',
      '등불 색 희망:',
      '매다는 장식 이미지 (있다면 오픈채팅으로 전달):',
      '공개 전 검수 여부:',
      '스크린 사용 여부와 기기:',
    ],
  },
  photozone: {
    name: '포토존 프레임',
    lines: [
      '프레임 PNG (투명 배경, 여러 장 가능 — 오픈채팅으로 전달):',
      '촬영 방식 (카메라 / 사진 올리기 / 둘 다):',
      '기본 카메라 (전면 / 후면):',
      '결과물에 넣을 워터마크 문구:',
      '※ 방문자 사진은 폰 안에서 합성되고 서버에 올라가지 않습니다.',
    ],
  },
  cheer: {
    name: '영상회 응원',
    lines: [
      '상영 화면 비율 (16:9 / 4:3):',
      '한 화면에 띄울 말풍선 수 (1~10, 6~8개 권장):',
      '교체 간격 (3~15초):',
      '이름 표시 여부 / 1인 입력 수 / 글자 수:',
      '공개 전 검수 여부:',
      '엔딩크레딧 사용 여부:',
      '상영 장비 (OBS·프리즘 등 브라우저 소스를 쓸 수 있는지):',
    ],
  },
  poll: {
    name: '실시간 투표',
    lines: [
      '결과 공개 방식 (실시간 / 투표 후 / 마감 후):',
      '득표 수 표시 여부:',
      '결과 모양 (막대 / 하트):',
      '현황 화면(스크린) 사용 여부와 기기:',
      '※ 문항과 보기는 운영 전 관리자 페이지에서 직접 등록해 주시면 됩니다.',
    ],
  },
  stamp: {
    name: '방문 스탬프',
    lines: [
      '도장 칸 수와 각 칸 이름:',
      '도장·판 배경 이미지:',
      '선물 (없음 / 확정 지급 / 응모):',
      '하루마다 초기화 여부:',
      '현장 암호 자릿수 (기본 4자리):',
      '교환 확인에 쓸 스태프 기기 (확정 선물일 때):',
      '※ 암호는 관리자 페이지에서 보고 언제든 새로 만들 수 있습니다.',
    ],
  },
  quiz: {
    name: '최애 모의고사',
    lines: [
      '문항 수와 유형 (객관식 / 주관식):',
      '점수 구간별 칭호:',
      '선물 (없음 / 커트라인 확정 / 응모):',
      '정답 공개 (공개 안 함 / 제출 후 전체 / 틀린 것만):',
      '제한 시간:',
      '※ 문항과 정답은 관리자 페이지에서 직접 등록해 주시면 됩니다.',
      '　(정답은 손님 화면에 내려가지 않습니다)',
    ],
  },
}

/**
 * **주문 제작** — 서비스 목록에 없는 것을 만들어 달라는 문의.
 * `ServiceId` 가 아니라서 `SERVICE_FORM` 밖에 따로 둔다 (서비스가 아니라 요청 종류다).
 * 여기서 묻는 건 딱 셋이다 — **무엇을 · 누가 조작하고 · 무엇이 보이는지.**
 * 그 셋이 없으면 견적을 낼 수 없고, 그 셋만 있으면 나머지는 대화로 채워진다.
 */
export const CUSTOM_FORM = {
  name: '주문 제작',
  lines: [
    '만들고 싶은 것 (어떤 이벤트에서 어떻게 쓰나요):',
    '조작하는 사람 (방문자 폰 / 스태프 기기 / 스크린 — 여럿이면 다 적어 주세요):',
    '화면 흐름 (손님이 무엇을 하면 무엇이 보이나요):',
    '비슷하다고 생각한 서비스나 사례:',
    '이미 준비된 자료 (이미지·문구·명단 등):',
  ],
}

/** 화면에 뜨는 순서 — 랜딩의 서비스 순서와 같게 둔다 */
export const FORM_ORDER: ServiceId[] = [
  'tarot',
  'luckydraw',
  'photocard',
  'rolling',
  'wish',
  'photozone',
  'cheer',
  'poll',
  'stamp',
  'quiz',
]

/**
 * 고른 서비스로 양식을 만든다.
 *
 * 공통 칸(일정·계정·디자인)은 **서비스를 몇 개 고르든 한 번만** 나온다 — 슬롯이 여럿이어도
 * 행사는 하나고, 같은 걸 두 번 묻는 양식은 채우다 만다.
 */
export function buildInquiry(picked: ServiceId[], custom = false): string {
  const names = [...picked.map((id) => SERVICE_FORM[id].name), ...(custom ? [CUSTOM_FORM.name] : [])]
  const out: string[] = []

  out.push(`[문의] ${names.length ? names.join(' · ') : '서비스 미정'}`)
  out.push('')
  out.push(LINE)
  out.push('')
  out.push('1) 일정')
  out.push('')
  out.push('희망 운영일 (행사기간, 마지막 날짜 기입 필수):')
  out.push('')
  out.push('자료 전달 예정일:')
  out.push('')
  out.push('시연/검수 희망일:')
  out.push('')
  /*
   * 긴급 여부는 **손님이 고르는 값이 아니라 위 두 날짜에서 나오는 값**이다.
   * 고르게 두면 "일반" 을 골라 놓고 3일 뒤 검수를 원하는 일이 생긴다.
   */
  out.push('※ 긴급 여부(일반 / 10일 이내 / 3일 이내)는 자료 전달 예정일과')
  out.push('　시연/검수 희망일 사이 기간으로 계산됩니다.')
  out.push('')
  out.push(LINE)
  out.push('')
  out.push('2) 관리자 로그인 정보')
  out.push('')
  out.push('관리자 이메일:')
  out.push('')
  out.push('관리자 비밀번호:')
  out.push('')
  out.push(LINE)
  out.push('')
  out.push('3) 디자인 (공통)')
  out.push('')
  out.push('행사명:')
  out.push('')
  out.push('디자인 요소(로고, 포인트 컬러, 버튼 스타일 등):')
  out.push('')
  out.push('배경 이미지/파일(있다면 오픈채팅으로 전달):')
  out.push('')
  out.push(LINE)
  out.push('')

  if (names.length) {
    out.push('4) 서비스별 설정')
    out.push('')
    for (const svc of [...picked.map((id) => SERVICE_FORM[id]), ...(custom ? [CUSTOM_FORM] : [])]) {
      out.push(`■ ${svc.name}`)
      out.push('')
      for (const line of svc.lines) {
        out.push(line)
        // `※` 안내는 답을 적는 칸이 아니라 빈 줄을 안 넣는다
        if (!line.startsWith('※') && !line.startsWith('　')) out.push('')
      }
      // `※` 로 끝난 블록은 빈 줄 없이 구분선에 붙는다 — 한 줄 띄워 준다
      if (out[out.length - 1] !== '') out.push('')
      out.push(LINE)
      out.push('')
    }
  } else {
    out.push('4) 서비스별 설정')
    out.push('')
    out.push('쓰고 싶은 서비스 (아직 못 정하셨다면 하시려는 이벤트를 적어 주세요):')
    out.push('')
    out.push(LINE)
    out.push('')
  }

  out.push('5) 기타')
  out.push('')
  out.push('추가 요청 사항:')
  out.push('')

  return out.join('\n')
}
