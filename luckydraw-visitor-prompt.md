# 럭키드로우 방문자 화면 프롬프트 — 내용 중심 (진행 · 당첨 · 확인 · 배송 모달)

> 디자인(색·폰트·여백·그림자 등)은 **지정하지 않는다.** 아래는 각 화면에 **반드시 들어가야
> 하는 내용·요소·동작**만 정리한 명세다. 시각 디자인은 만드는 쪽이 알아서 정한다.
> **너비는 고정**(휴대폰 폭 한 컬럼, 가운데). 모든 문구는 한국어, 금액·판매 표현 금지.

---

## 지시문 (이 아래를 그대로 전달)

**생일카페 이벤트**에서 손님이 **현장 휴대폰으로 경품을 뽑는** 화면 흐름을 만들어라. 아래
4개 화면(진행 → 당첨 → 확인 → 배송 모달)이 담아야 할 **내용과 동작**을 충실히 구현한다.
시각 스타일은 자유지만 **폭은 고정된 모바일 한 컬럼**으로 한다.

### 0. 맥락

- 손님이 카페에서 **세로 휴대폰**으로 본다. 화면은 **고정 너비 한 컬럼**.
- 이벤트마다 **로고·배경·카드 앞/뒷면 이미지**가 따로 올라온다(플랫폼이 제공). 화면 가운데에
  이벤트 이미지가 비치는 영역 위에 컨트롤이 얹힌다고 가정.
- **뽑는 건 스태프만 한다.** 손님 화면에 뽑기 버튼은 보이되, 스태프가 로그인하지 않은
  상태에선 **비활성**이다(주소가 새어 나가도 재고가 안 타게).
- 하단 탭바 같은 앱 셸은 **없다**(추첨 한 흐름만).

---

### 1. 화면 — 진행 (뽑기)

손님 앞에 놓인 기본 화면.
- (재고가 얼마 안 남았으면) **"N개 남았어요"** 안내.
- **수량 고르기**: `−` / 숫자 입력 / `+` 카운터 + 빠른 선택 버튼(예: 1개·5개·10개). 1개
  이상, 최대치 제한.
- **뽑기 버튼**(라벨은 이벤트가 정함, 기본 "뽑기"). 스태프 미로그인 시 **비활성**, 뽑는
  중이면 "뽑는 중…".
- **리허설이면** "지금은 리허설이에요. 뽑아도 실제 재고는 줄지 않아요" 안내.
- **마감이면** 뽑기 대신 마감 안내 문구만.
- 화면 아래쪽(눈에 안 띄게)에 **관리자 진입 링크** — 상태에 따라 `관리자로 로그인` /
  `관리자 페이지로 이동`.
- (설정돼 있으면) 화면 구석에 아주 흐린 **제작사 표기** 한 줄.

### 2. 화면 — 당첨 (결과 공개)

뽑기 직후. **연출이 핵심**이다.
- 제목 **"당첨 결과"**.
- 뽑은 항목이 한 칸씩. 각 칸 표시는 이벤트 설정을 따른다: `N등` / 상품명 / `N등 · 상품명`.
- 여러 개면 칸을 그리드로(개수에 따라 폭이 달라져도 됨).
- **비싼 등수(하이라이트)는 덮여 있어 손님이 직접 긁어 확인**한다. 덮개에는 긁기 마크가 있고,
  긁으면 **폭죽** 연출이 화면을 덮는다. (하이라이트가 아닌 항목은 처음부터 순서대로 공개.)
- 아래에 **"전체 결과 보기"** 버튼 → 확인 화면.

### 3. 화면 — 확인 (전체 결과)

무엇을 받았는지 정리해서 보여준다.
- 제목 **"전체 결과"**.
- **상품별 묶음 목록**: 라벨(위 표시 규칙과 동일) + **"N개"**.
- **리허설이면** "리허설이라 재고는 줄지 않았어요" 한 줄.
- **배송이 필요한 상품이 있으면** **"배송 정보 입력하기"** 버튼 → 배송 모달.
- **"처음으로"** 버튼(다시 진행 화면으로).

### 4. 배송 모달

배송 대상 상품이 있을 때 위에서 띄운다. **고정 너비 카드형 모달**(화면을 덮는 시트).
- 헤더: **"배송 정보 입력"** + "당첨 상품 수령을 위해 아래 정보를 입력해주세요."
- **배송 대상 상품** 카드: 상품마다 한 줄 — `N등 · 상품명` ... `N개`.
- 입력: **받는 분**(이름), **연락처**, **주소**.
- **개인정보 수집·이용 동의** 체크 — 목적은 경품 배송, 배송이 끝나면 폐기된다는 안내.
- 하단: **취소** / **보내기**. 보내기는 필수 입력 + 동의를 다 채워야 **활성**. 보내는 중엔
  "보내는 중…".
- **제출 성공**: "보냈어요 · 배송 정보가 스태프에게 전달됐어요" + 닫기.
- 안내: 제출한 값은 보안상 **다시 못 읽는다**(본인도). 틀리면 스태프에게 말하면 된다.

---

### 5. 공통 동작 규칙 (요약)

- **폭 고정**(휴대폰 한 컬럼, 가운데 정렬).
- 뽑기는 **스태프(로그인)만**. 남은 재고 합계만큼만 뽑힌다.
- 하이라이트 등수만 **덮여 있고 긁어야** 보인다. 나머지는 순서대로 등장.
- 흐름은 **진행 → (뽑기) → 당첨 → 전체 결과 → (필요 시) 배송 모달 → 처음으로**.
- 배송 정보는 손님이 직접 입력하고, **제출 후 수정 불가**.
